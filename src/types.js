// card types
const CARD_SWITCH_REQUEST = 'CARD_SWITCH_REQUEST';
// TODO move to card
const CARD_SIDE_BACK = 'CARD_SIDE_BACK';
const CARD_SIDE_FRONT = 'CARD_SIDE_FRONT';

// board types
const BOARD_SET_CARDS = 'BOARD_SET_CARDS';

// game/match types
const MATCH_START = 'MATCH_START';
const PLAYER_SET_ACTIVE = 'PLAYER_SET_ACTIVE';
const PLAYER_INCREMENT_COUNT = 'PLAYER_INCREMENT_COUNT';
const ROUND_INCREMENT_COUNT = 'ROUND_INCREMENT_COUNT';
const MATCH_SET_WINNER = 'MATCH_SET_WINNER';

export default {
   ROUND_INCREMENT_COUNT,
   MATCH_SET_WINNER,
   PLAYER_SET_ACTIVE,
   PLAYER_INCREMENT_COUNT,
   MATCH_START,
   BOARD_SET_CARDS,
   CARD_SWITCH_REQUEST,
   CARD_SIDE_BACK,
   CARD_SIDE_FRONT,
};
